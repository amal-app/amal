import { HOME_SCREEN } from '@/assets'
import React from 'react'

import { View, Image } from 'react-native'

const HomeScreenImage = () => {
  return (
    <View>
        <Image source={HOME_SCREEN} />
    </View>
  )
}

export default HomeScreenImage